---
layout: layouts/base.njk
title: "სიახლეები | GEBA"
lang: ka
nav_active: news
alt_url_en: "/pages/news.html"
permalink: "/pages/news-ge.html"
---
<div class="page-hero">
  <div class="page-hero-inner">
    <h1>სიახლეები</h1>
    <p>GeBA-ს განახლებები, ღონისძიებები და განცხადებები.</p>
  </div>
</div>

<div class="section-block">
  <div class="news-strip news-strip--full">
    {% for item in collections.news | reverse %}
    <article class="news-card">
      {% if item.data.cover %}
        <img src="{{ item.data.cover }}" alt="{{ item.data.title_ka | escape }}" class="news-card-img">
      {% else %}
        <div class="news-card-img"></div>
      {% endif %}
      <div class="news-card-body">
        {% if item.data.date %}<p class="news-date">{{ item.data.date | date("dd MMM yyyy") }}</p>{% endif %}
        <p class="news-headline"><a href="{{ item.data.permalink_ka }}">{{ item.data.title_ka }}</a></p>
        <a class="news-read-more" href="{{ item.data.permalink_ka }}">ვრცლად →</a>
      </div>
    </article>
    {% endfor %}
  </div>
</div>
